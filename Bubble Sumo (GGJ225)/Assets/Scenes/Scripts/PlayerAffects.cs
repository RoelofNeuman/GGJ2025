using UnityEngine;

public class PlayerAffects : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //stay in place
    //be source of bubbles (currently teh boxes)
    //if bubbles get to a certain point push player or enemy back 

}
